# Pending

Running list of everything currently hanging — open `/discuss` proposals awaiting a decision, tasks flagged as "next up" but not yet started, and open decisions. Read this (or run `/pending-task`) any time you need to know what's outstanding, without needing prior conversation context.

Entries are added by the `discuss` skill when a proposal is presented (removed on approval or explicit decline), or added manually for a task/decision that isn't yet a formal proposal. Proposal entries carry their `PROP-YYYYMMDD-NN` ID inline (e.g. `[PROP-20260903-01]`) — that ID survives the entry's removal here since it lives on in the commit message(s) the `commit` skill produces. Newest first.

## Open items

- **2026-09-03 — decision** — Which of the three remaining leave-domain pieces to tackle next: leave balance calculation, the approval workflow, or the `LeaveRequest` submission UI. All three are unblocked (data model ready); no `/discuss` proposal written yet for any of them. See `PROJECT_STATUS.md` → "In progress / next steps discussed with the user".
